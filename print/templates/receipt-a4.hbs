<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8" />
  <title>Facture {{factureNum}}</title>
  <style>
    body { font-family: Arial, sans-serif; font-size: 12pt; }
    h1 { font-size: 16pt; margin: 0 0 8px; }
    table { width: 100%; border-collapse: collapse; margin-top: 8px; }
    th, td { border-bottom: 1px solid #ddd; padding: 6px; text-align: left; }
    tfoot td { font-weight: bold; }
  </style>
</head>
<body>
  <h1>Facture {{factureNum}}</h1>
  <div>
    {{#if entreprise}}
      <div><strong>{{entreprise.nom}}</strong></div>
      {{#if entreprise.rccm}}<div>RCCM: {{entreprise.rccm}}</div>{{/if}}
      {{#if entreprise.impot}}<div>Impôt: {{entreprise.impot}}</div>{{/if}}
      {{#if entreprise.tel}}<div>Tel: {{entreprise.tel}}</div>{{/if}}
    {{/if}}
    <div>Client: {{client}}</div>
    <div>Date: {{date}}</div>
    <div>Taux: {{taux}} FC</div>
  </div>
  <table>
    <thead>
      <tr><th>Code</th><th>Article</th><th>Qté</th><th>PU</th><th>Total</th></tr>
    </thead>
    <tbody>
      {{#each lines}}
        <tr>
          <td>{{code}}</td>
          <td>{{nom}}</td>
          <td>{{qty}}</td>
          <td>{{pu}}</td>
          <td>{{total}}</td>
        </tr>
      {{/each}}
    </tbody>
    <tfoot>
      <tr><td colspan="4">TOTAL</td><td>{{total}}</td></tr>
    </tfoot>
  </table>
</body>
</html>
  